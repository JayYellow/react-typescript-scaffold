import React from 'react';

import style from './index.css'

const Home = (props: any) => {
  return (
    <div className={style.home}>
      <span>hello</span>
    </div>
  );
};

export default Home;
